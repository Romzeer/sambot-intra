import { PriorityBgDirective } from './priority-bg.directive';

describe('PriorityBgDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityBgDirective();
    expect(directive).toBeTruthy();
  });
});
